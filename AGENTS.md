# 发布完成标准

- SIP 电话规则和分阶段验收见 `deploy/SIP-CALLS.md`。适配前必须阅读；单客户端登录、立即撤权挂机、共享模块及来电首接规则不可用模拟行为代替。

- 用户要求每次修复完成都同步：GitHub 源码、Latest Release 一键部署安装包、实际主机。
- 测试通过后增加 VERSION，用相同提交创建版本标签。标签 CI 构建并发布 amd64/arm64 安装包和 release.json。
- 主机使用该正式发布资产更新；保留账号、会话、模块配置和回滚备份，先确认没有正在执行的 eSIM 任务。
- 验证远端提交、Latest 标签、两种架构资产摘要、主机实际运行二进制及静态资源、模块状态。
- 源码推送不等于一键部署更新。任何一项未完成都明确说明，不报告“全部同步”。
- 功能验收与发布同步分开：3HK 注册、实际通话或 MMS 未验收时必须保留未完成说明。
