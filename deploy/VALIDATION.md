# 实机验证

## 2026-09-24 · v1.1.0

安装包对应提交：`c5788697ddeb42f71032294966f355e9904de91f`。

| 项目 | 结果 |
| --- | --- |
| Ubuntu 24.04 / amd64 | 使用已校验 Release 安装包，从新版目录结构的 v1.0.0 更新至 v1.1.0 |
| 更新备份 | 数据库、配置、应用状态和原发布链接已备份；数据库备份可读取 |
| 数据保留 | 管理员及密码数据、独立密码、显示配置指纹一致；会话记录数量一致 |
| 服务 | rykvo-auth、Nginx、PostgreSQL 均为 active |
| 路由隔离 | 局域网根路径 200，域名根路径 404，域名 /gly 为 200；未登录模块 API 和业务资源为 401 |
| 模块发现 | 自动识别一个 EC20F，自动建档为模块 01，服务账号获得指定硬件节点访问权限 |
| 当前卡片 | 用户确认当前插入普通运营商 SIM；eSIM 管理应用 SELECT 返回 6A82，符合非 eUICC 卡的结果。9eSIM 换卡后另行验证，不将此结果认定为 9eSIM 故障 |

未执行切卡、安装或删除 eSIM、密码重置、系统重启、卸载。
本次不代表 9eSIM 配置读写、多设备实机、全新安装、真实卸载或 arm64 实机验证完成。浏览器登录后的业务页视觉检查仍待完成。

## 2026-09-24 · v1.1.1

安装包对应提交：`04d3ab33fc05e78b68bb5dea8f4fe99e75c440bb`，发布为预发布版本。

| 项目 | 结果 |
| --- | --- |
| CI | 201 项前端测试、12 项部署测试、Go 测试及静态检查通过；amd64、arm64 安装包上传摘要与本地一致 |
| 实机更新 | Ubuntu 24.04 / amd64 从 v1.1.0 更新至 v1.1.1；使用 SHA-256 校验后的 Release 安装包；56 项站点文件与清单一致 |
| 数据保留 | 更新前后管理员、独立密码、显示配置、会话和模块档案指纹一致；模块比较排除发现时间及端点代次；默认路由不变 |
| 备份与服务 | 本次数据库备份可读取；rykvo-auth、Nginx、PostgreSQL 均为 active |
| 路由隔离 | 局域网根路径 200，域名根路径 404，域名 /gly 为 200；未登录模块 API 和业务脚本为 401 |
| 页面 | 更新后原会话继续有效；真实普通 SIM 未返回号码时，列表和详情显示 ICCID；已检查浅色桌面和 390px 窄屏，检查期间未捕获控制台错误或警告 |
| 9eSIM 02 | 早期 AT 返回 CME 10 / CME 0、QMI 返回 no-atr-received。随后用户反馈恢复，已在真实网页确认一个 Wireless 配置、已启用状态和 ICCID；未返回号码，仍显示无服务。本轮未单独复读 EID |
| 9eSIM 对照 | 用户确认新增 04 也为 9eSIM；通过已部署程序的只读接口取得 EID 和一个已启用配置，网页同步显示配置及本机号码；该模块已漫游注册 LTE。未验证配置切换、下载或删除 |

读取卡内配置不要求蜂窝漫游注册。用户确认该卡通过 USB 读卡器和参考项目可读，目前不能据此将本次模块初始化失败归为卡片损坏。

04 的读取成功及 02 后续恢复说明现有读取链路可用于这两张卡。02 早期初始化失败的原因尚未确定，不认定卡片损坏；不将本次各返回一个配置推断为所有 9eSIM 的容量限制。02 恢复检查期间未修改或重新部署程序，未执行复位。

参考 VoCat 的通道恢复流程和 NekokoLPA2 的读卡器初始化、多 AID 选择流程；尚未向 02 执行 SIM 子系统复位，也未切换、安装或删除配置。后续按实际芯片响应验证，不把模拟配置列表测试计为实机通过。

本次深色实机视觉检查待完成。数据连接、漫游控制、Wi-Fi 通话和 MMS 仍未接通；未执行系统重启、密码重置或卸载。

## 2026-09-24 · v1.1.2

安装包对应提交：`d4f47e7ea8ec4fa3b1a318b0f0a2a8c5d3163cb2`，发布为预发布版本。

- GitHub CI：204 项前端测试、12 项部署测试、Go 测试与 vet、PostgreSQL 集成测试及竞争检查通过。
- amd64 / arm64 安装包摘要与 Release 资产一致；实机使用校验后的 amd64 包更新。
- 更新前后账号、独立密码、显示配置、会话、模块档案指纹和默认路由一致；数据库备份可读取；三个服务 active；56 项站点文件与清单一致。
- 局域网根路径、域名 /gly 返回 200，域名根路径 404；未登录模块 API 和业务脚本返回 401。
- 真实页面原会话继续有效，四个模块在线；04 展示两个配置及正确启用状态，底部不再保留“已完成”。已检查浅色桌面、390px 窄屏的列表、添加表单和切换入口，控制台未捕获错误或警告。
- 新任务保存最小核对信息；下载激活码和确认码不入库。操作后的重试只读取状态，不重放写卡指令；后续读到匹配结果可消除不确定提示。旧任务缺少核对目标时不猜测结果。

未为验证页面执行下载、切换、删除或复位；新进度条完整过程及深色实机视觉检查仍待实际操作验证。虚拟机 USB 稳定性按用户要求暂缓，未停止 ModemManager 或修改硬件访问权限，不宣称已修复连接稳定性。

## 2026-09-24 · v1.1.3

安装包对应提交：`2f24cfb0baf1acb3f8aaa16fa271993c0902d7d6`，发布为预发布版本。

- GitHub CI：205 项前端测试、12 项部署测试、Go 测试与 vet、PostgreSQL 集成测试及竞争检查通过。
- amd64 / arm64 安装包摘要与 Release 资产一致；实机使用校验后的 amd64 包更新至 v1.1.3。
- 更新前后账号、独立密码、显示配置、会话、模块档案指纹和默认路由一致；数据库备份可读取；三个服务 active；56 项站点文件与清单一致。
- 局域网根路径、域名 /gly 返回 200，域名根路径 404；未登录模块 API 和业务脚本返回 401。
- 真实页面会话保留，四个模块在线；普通卡 01、03 隐藏“添加 eSIM”，已识别 eUICC 的 02、04 保留入口。按芯片能力判断，不通过点击失败推断。
- 已检查浅色桌面和 390px 窄屏，控制台未捕获错误或警告；深色实机视觉检查待完成。

本次未执行射频切换、下载、启停或删除卡内配置。网络选择、数据连接及漫游控制、VoWiFi 尚未接通；没有通过单独设置飞行模式冒充 Wi-Fi 通话。虚拟机 USB 稳定性排查仍按用户要求暂缓。

## 2026-09-24 · v1.1.4

安装包对应提交：`dc27b0892422398ba36a4f354b63a3449176fadb`。

- GitHub CI：206 项前端测试、12 项部署测试、Go 测试与 vet、PostgreSQL 集成测试及竞争检查通过。
- 实机使用校验 SHA-256 后的 amd64 包更新至 v1.1.4；56 项站点文件与清单一致。
- 更新及搜索后，账号、独立密码、显示配置、会话、模块档案指纹和默认路由一致；数据库备份可读取；三个服务 active。
- 局域网根路径、域名 /gly 返回 200，域名根路径 404；未登录模块 API 和业务脚本返回 401。
- 在真实网页登录会话中，仅对 01 提交一次运营商搜索，任务成功并返回 5 条记录，包含中国移动、联通、电信、广电；禁止接入项禁用，当前网络显示勾选。
- 搜索后 01 仍为自动选网，四个模块在线；未提交自动或手动选网写操作，不将搜索成功等同于手动注册成功。
- 已检查浅色桌面和 390px 窄屏的设置、搜索进度及结果，控制台未捕获错误或警告；深色实机视觉检查待完成。

本次未下载、切换或删除 eSIM，未执行 CFUN、USB 复位、系统重启或卸载。实际自动/手动选网切换仍待用户指定目标后验证；数据连接及漫游控制、VoWiFi 和 MMS 尚未接通。部署临时目录已清理，更新备份保留。

## 2026-09-24 · v1.1.5

安装包对应提交：`08459da922a7e64fa98b2a074327d378bb350b57`。

- GitHub CI：207 项前端测试、12 项部署测试、Go 测试与 vet、PostgreSQL 集成测试及竞争检查通过。
- amd64 包校验后实机更新，amd64 / arm64 Release 资产摘要一致；账号、配置、会话、模块档案和默认路由在更新检查时一致。
- 实机对 01 执行一次运营商搜索；搜索中可返回并重新进入网络选择，结果返回后可再次打开，不重复发起扫描。
- 该版本未解决选网任务替换搜索任务后列表消失的问题；相关未完成修改另行保留，未混入 v1.1.6。

## 2026-09-24 · v1.1.6

安装包对应提交：`dbfdc8410d136dcc9ff8f32de1619318df8eaf3e`，预发布版本。

- GitHub CI：208 项前端测试、17 项部署测试、Go 测试与 vet、PostgreSQL 集成测试及竞争检查通过。
- amd64 / arm64 安装包摘要与 Release 资产一致；实机使用校验后的 amd64 包更新，56 项站点文件与清单一致。
- 后端账号仍为 rykvo_voice。新套接字仅该账号及 root 可访问，固定只读查询进程按需启动；验证无关账号被拒绝、搜索命令被白名单拒绝，未向模块执行搜索。
- 按 IMEI 哈希与数据库逐台核对，03、04 均已注册网络；应用账号通过新通道读取身份、当前卡槽 ICCID、注册状态和信号成功。页面从 3 在线 / 2 读取异常恢复到 5 在线 / 0 读取异常。这里的在线指模块可读取，不表示所有卡都已注册网络。
- 诊断时发现 USB 编号不等于页面编号。先前归到 03 的 registration-denied 实际属于无漫游的 02；不把 USB 编号当模块身份，不据此改动卡片或网络设置。
- 03、04 的 AT 通道基础查询仍超时，04 的选网任务曾等待约 3 分钟后变为结果待确认。搜索记录本身成功。检查时段未发现 USB 断开或重置；尚未证明 AT 停滞由虚拟机、固件或端口争用中的哪一项造成。QMI 状态恢复不等于选网或 eSIM 控制恢复。
- 账号、独立密码、显示配置、会话及默认路由指纹一致；模块编号、硬件身份、端点、标签和创建时间与备份一致。03、04 的型号字段变为 QMI 返回的通用名称，因此完整模块档案指纹发生变化，并非绑定或账号数据丢失。
- 数据库备份可读取；应用、QMI 套接字、Nginx、PostgreSQL、ModemManager 均 active。局域网 / 与域名 /gly 为 200，域名根路径为 404，未登录业务脚本和模块 API 为 401。
- 已检查真实会话、浅色桌面和 390px 窄屏，控制台未捕获错误或警告。未修改 CSS；现有页面没有主题切换入口，深色实机视觉检查仍待完成。

仅重启网页后端进行更新；未重启虚拟机、停止 ModemManager、复位 USB/射频、下载/切换/删除 eSIM 或执行卸载。数据连接、漫游控制、VoWiFi、MMS 等未在本次接通；arm64 实机运行及全新安装、真实卸载仍未验证。

## 2026-09-25 · v1.1.7

安装包对应提交：`4e7ed5c46e7d6dc1e369188f33f400da35520753`，预发布版本。

- 删除手动搜网、选网入口、接口、执行代码及过期测试，不叠加隐藏逻辑。旧选网任务保留数据库历史，但不恢复到页面；原计划的搜索结果保留改动随功能取消。
- 卡片通道错误优先于读取中提示；数字运营商名称用匹配 MCC/MNC 的 QMI 名称补齐，不发送选网或格式修改命令。
- GitHub CI `36025129113` 通过：208 项前端测试、17 项部署测试、Go 测试与 vet、PostgreSQL 集成、竞争检查和双架构构建。未在本地运行适配测试。
- amd64 / arm64 Release 摘要校验通过；实机以 amd64 包更新至 v1.1.7，56 项站点文件符合清单。备份 `20260924-161054-e68ed1` 可读取。
- 账号、独立密码、显示配置、会话、模块稳定档案及默认路由校验一致；模块校验排除动态型号、采集时间和设备世代。服务账号仍为 rykvo_voice，应用、QMI 套接字、Nginx、PostgreSQL 和 ModemManager 均 active。
- 根据用户取消手动选网的要求，核对 IMEI 身份和无活动任务后，仅将 04 从手动恢复为自动，保留 LTE 制式。回读五个模块均为 automatic；04 注册状态为 registered。未重启模块或虚拟机，未修改卡内配置。
- 原网页登录会话继续有效；01 显示 CMCC，不再显示 46000；设置中无网络选择。02 仍显示两个真实配置和添加入口。04 不再显示旧选网提示或持续读取，而显示实际的设备读取超时。
- 已检查浅色桌面、390px 窄屏的蜂窝设置及模块、电话、信息、通用页面导航，控制台无错误或警告。未增加 CSS、事件或定时器；现有页面无主题切换入口，深色实机视觉检查仍待完成。
- 局域网 / 与域名 /gly 为 200，域名根路径为 404，未登录业务脚本和模块 API 为 401。

04 的 AT 通道仍超时，恢复自动选网未恢复读卡；03 同类 AT 问题也未证明已恢复。QMI 注册正常不等于 eSIM 控制正常。检查时段无 USB 断开/重置证据，根因仍未定位；已请求单独重启 04 的确认，尚未执行。未把页面修正宣称为硬件稳定性修复。

### 后续：03 / 04 单模块恢复

- 用户确认此类故障重启模块后，先后对 04、03 执行单设备 QMI operating-mode reset。每次执行前核对 IMEI 哈希、USB 物理端点及无活动写任务；未使用恢复出厂或整机重启。
- 04 在 UTC 16:16:19 断开、16:16:39 重新枚举，随后网页恢复两个 eSIM 配置及当前号码。03 在 UTC 16:17:55 断开、16:18:19 重新枚举，随后普通 SIM 恢复显示一条 ICCID 记录，无添加 eSIM 入口。
- 两次分别只有目标设备的 USB devnum 变化；虚拟机 boot ID、默认路由不变，其他模块未重新枚举，应用和 ModemManager 未重启。
- 两个模块回读均为自动选网、已注册，页面恢复 5 个可读取模块。未执行卡内下载、启停、删除或通知重试。
- 03 重启前 SIM 为 ready，QMI 已注册，但主 AT 口被 ModemManager 占用、次 AT 口查询超时；健康设备的主 AT 口也被同一服务占用。因此仍未证明占用就是根因，也未证明与虚拟机或固件的因果关系。

本次是运行恢复，没有新增自动重启逻辑或修改运行程序，仍使用 v1.1.7。单模块重启恢复读卡不等于长期稳定性问题已根治。

### 后续：02 单模块恢复

- 02 随后也出现读卡超时：主 AT 口被占用，次 AT 口基础查询超时；QMI 仍可读取，返回注册被拒绝、卡片应用状态 illegal。此处记录原始状态，不据此断定卡损坏或与超时存在因果关系。
- 按用户此前确认的故障处理方式，核对 02 的 IMEI、USB 端点及无活动写任务后，只重启 02。UTC 16:20:50 断开，16:21:09 重新枚举；仅该设备 devnum 改变，虚拟机 boot ID 和默认路由不变。
- 网页恢复 BetterRoaming、Wireless 两个配置和添加入口，Wireless 保持启用，选网仍为自动。无服务与读卡超时分开核实，未切换配置或尝试取得漫游服务。
- 模块 ID、标签和端点保持不变，其他模块未重新枚举，服务均 active。没有程序变更，运行版本仍为 v1.1.7；多设备先后出现超时的根因仍待定位。

## 2026-09-25 · 多模块故障排查与 v1.1.8 预发布

安装包对应提交：`3c612680768b7bdcfd5710f9cc8a5972f2bde0ad`。未部署到当前虚拟机；运行版本仍为 v1.1.7。

- 用户确认 16 个模块共用一块 USB 板；页面错误数随接入增加，但尚未证明因果关系。
- UTC 16:33 检查：16 个 QMI 设备仍在 sysfs；抽查 wdm3 / wdm5 的 DMS 身份查询等待 7 秒超时，wdm6 在约 0.2 秒返回。查询仍经 qmi-proxy，不称为绕过代理的独立验证。
- 16:28 的 02 / 03 USB 断开和重新枚举，与用户报告重启相邻；16:29–16:30 又接入 4 个模块。不把这些记录直接归为故障掉线或供电不足。
- UTC 16:40 采样：CPU 99%–100% 空闲，可用内存约 7140 MiB / 7894 MiB，swap 未使用，应用 TasksCurrent=16 / TasksMax=9374。检查时段没有 OOM、过流、控制器故障或 QMI socket 超额记录。此结果不排除设备固件、QMI 客户端资源、USB 直通、共享供电或访问并发问题。
- 核对 VoCat 提交 `484cd236dd543e2ba142cf1da2c5808ee8e89a6e`：应用常量 DefaultDeviceLimit=5、MaxDeviceLimit=10。该配额不证明 Linux、虚拟机或 USB 存在 5 台硬上限。
- 新增有身份核对、设备互斥、持久化冷却和失败停止条件的 EC20 自动恢复。代码已同步仓库；CI `36029493424` 通过前端、18 项部署工具、Go 测试/vet、隔离数据库集成、竞争检查及双架构安装包构建。未在本地运行适配测试。
- 当前没有执行自动恢复实机测试、部署、单模块重启、整机重启、卡片写入或模式切换；没有用重启掩盖现场。新恢复文案的真实桌面/窄屏/深浅色视觉检查及长期稳定性验证仍待部署后完成。

恢复逻辑不等于根因修复；实体主机可以去掉虚拟机直通这一层，但不保证解决共享供电或软件并发问题。需固定硬件条件做并发/接入数量对照，避免同时更换多个变量后误判。

## 2026-09-25 · v1.1.9 部署

安装包提交：`6e05eea756ddc601b0bc79f8cf6b48c50601da56`，预发布版本。包含 v1.1.8 自动恢复规则及无卡识别修复。

- CI `36031146652` 通过：209 项前端、18 项部署工具、Go 测试/vet、隔离 PostgreSQL 集成、竞争检查与双架构构建。未在本地运行适配测试。
- amd64 / arm64 安装包 SHA-256 与 Release 资产一致。UTC 17:04 使用校验后的 amd64 包更新，运行版本 1.1.9，56 项站点文件符合清单。备份 `20260924-170422-e1b259` 可读取。
- 账号、独立密码、显示配置、会话、模块稳定档案及默认路由指纹一致。服务仍以 rykvo_voice 运行；应用、QMI 套接字、Nginx、PostgreSQL、ModemManager 均 active。虚拟机 boot ID 和 16 个 USB 设备 devnum 未变。
- 部署前 QMI 确认 10、12 卡槽 absent；部署后真实页面 06–16 列表显示“无 SIM 卡”，10、12 详情一致，不再误报 eSIM 操作失败。02 有卡无漫游仍显示“无服务”，两个配置及添加入口正常。01、03、04、05 显示已注册网络，16 个模块可读取，异常 0。
- 自动恢复代码、本机助手和持久化表已部署。检查至 UTC 17:06 未触发恢复记录，未人为制造通信故障或重启健康模块；真实故障自动恢复及长期稳定性仍待验证。
- 原网页登录会话有效；已检查浅色桌面、390px 窄屏无卡弹窗及模块、电话、信息、通用导航，控制台无警告或错误。未增加 CSS、事件或定时器；深色规则已静态检查，现有页面无主题入口，深色实机视觉检查仍待完成。
- 局域网 /、域名 /gly 为 200，域名根路径 404；未登录模块 API、业务脚本 401。未写卡、切换网络、重启虚拟机或执行卸载。arm64 实机、全新安装和真实卸载仍未验证。

## 2026-09-25 · VoWiFi 前置读取

代码提交 `74e8a589396e201e8abd8193bb83fd1d7f2bf772`，CI `36033391712` 通过。仅使用 CI 候选包里的独立只读检查程序，不安装、不覆盖 v1.1.9 Release 或运行服务；候选包保留旧 VERSION 不表示它是已发布资产。

- 209 项前端、18 项部署工具、Go 测试/vet、隔离数据库集成、硬件竞争检查、双架构构建通过。适配测试未在本地运行。
- 候选 amd64 包 SHA-256 `7eea710a3e7f992402b1ec8f61d5f3ff6ed5cac008ccdc75acf3622878b9a7e4`，上传后再次核对，使用现有安全解包器。
- 虚拟机 UTC 17:23–17:25，依次核对 03 普通 SIM、02 和 04 的当前 9eSIM 配置。检查先验证设备世代与 IMEI 哈希，独占 AT 口，再读取 USIM EF_IMSI / EF_AD，前后比对 ICCID，关闭逻辑通道。输出只含可读状态、MCC/MNC 与推导域名，不输出卡标识或密钥。
- 三张卡最终均通过读取；04 首次超时，再次读取成功，未重启模块，尚未定位这次瞬时超时的具体环节。QMI DMS ICCID 查询返回读取失败，维护探针改用独占 AT 口读取当前 ICCID；不把该返回值当作无卡或触发恢复。
- 三个推导 ePDG 域名在虚拟机上均可解析。DNS 成功不代表运营商授权、IKE/IPsec 建立或 IMS 注册。
- 未发送真实 AKA 挑战，未改变 CFUN、PDP、SIM 配置、默认路由或应用权限。运行版本仍为 v1.1.9，应用与 Nginx active，虚拟机 boot ID 不变，活动写卡任务和自动恢复记录均为 0。

本阶段只验证 SIM 身份读取与协议解析基础。飞行模式联动、网络隧道、IMS 注册/续期、实际呼叫和音频尚未接通；页面开关仍禁用。本次无前端变更，不新增视觉验证结论。

## 2026-09-25 · 虚拟机网络现场核对

UTC 17:42 起，在 192.168.194.129 直接运行一次性 IKE_SA_INIT 诊断并读取 journal。此项是网络前置检查，不是 Rykvo 完整 VoWiFi 候选版本部署或注册测试。

- 正式服务仍为 v1.1.9、rykvo_voice 账号运行，应用/QMI 套接字/Nginx active。近 30 分钟应用 journal 无新记录；没有完整 VoWiFi 运行时，因此也没有运营商注册日志。16 个模块档案最近 60 秒内更新，无活动写卡任务、无自动恢复记录。
- 使用前次读卡推导的 02（310/240）、03（234/10）ePDG 域名，向 DNS 返回的公网 IPv4 地址发送有界 IKE_SA_INIT。UDP 500 和 4500 均收到匹配发起 SPI、交换类型、Message ID 与长度的响应，包含 SA/KE/Nonce。尚未验证所选算法、对端身份、EAP 或 IMS；不能把回应视为隧道建立或号码注册。
- 04（454/03）ePDG 域名经虚拟机系统解析器返回 127.0.0.1，IPv6 结果为其 NAT64 映射 64:ff9b::7f00:1。检查停止于非公网 IPv4 校验，没有向回环地址发送 IKE。此结果不证明香港地域限制、DNS 劫持或卡片故障中的任何一种原因。
- 未读取或发送 IMSI、ICCID、AKA 密钥；未设置飞行模式、改路由、下载/切换配置或重启设备。未替换正式服务，也未发布新的运行版本。

后续以虚拟机真实功能操作及脱敏连接日志验证，不用协议单测、DNS 成功或 IKE_SA_INIT 回应代替注册/通话验收。代码里的 EAP-AKA 层仍待接入真实 IKE/IPsec、IMS 和模块生命周期。

## 2026-09-25 · v1.2.0 候选：真实 IMS 注册

- 维护开发和编译在虚拟机临时目录进行。02 先完成 EAP-AKA、最终 MSK 对端 AUTH、双层 ESP 和 IMS Digest AKA，收到真实 SIP 200。75 秒维持测试完成一次续期及 60 秒 IKE 存活检查，随后注销并恢复原射频；另一次 20 秒回归通过。未写入、删除或切换 eSIM，未重启设备或虚拟机。
- 实测主 AT 口已被 ModemManager 打开，存在回复丢失；使用次 AT 接口后测试稳定。此证据仅解释本次 AT 通道冲突，不证明历史 QMI 故障均已定位。
- 修正 EPC 永久身份域名为 nai.epc；修正受保护 UDP 响应需经 UE server SPI/端口接收的问题。注册响应包含可验证的 AKA/IPsec 结果，不以 DNS、飞行模式或 IKE 成功代替注册成功。
- 接入原线路 PATCH 和 Wi-Fi 页面，复用模块互斥与前端订阅；正在连接、已连接与请求已接受分开。尚未进行生产页面验收或发布部署；本条记录时运行版本仍为 v1.1.9。
- VM 上 212 项前端测试、Go 测试和 vet 通过。竞争、隔离数据库、双架构构建交由 CI 验证。尚未验证所有运营商、长期稳定性、真实通话/音频与 IMS 短信。
