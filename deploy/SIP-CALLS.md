# SIP 电话适配

## 已确认规则（2026-09-26）

这是用户确认的产品规则。后续修改先核对本文，不从界面占位行为推断电话能力。

| 项目 | 规则 |
| --- | --- |
| 账号登录 | 一个账号只保留一个有效客户端注册。B 登录成功后撤销 A；A 的旧续注册、旧呼叫和迟到回调均失效。 |
| 顶号、改密、删除 | 立即停止相关声音、结束该账号通话和振铃、撤销旧注册。改密须新密码重新认证；删除后拒绝注册。 |
| 模块分配 | 账号与模块多对多，一个模块可以分给多个账号。 |
| 全部 | 从当前空闲且真正具备通话能力的全部模块中随机选择。 |
| 固定 | 只在勾选范围内选择空闲可用模块，绝不越界。 |
| 并发限制 | 每账号、每模块各一通电话；拨号、响铃、接通和未完成清理都占用资源。 |
| 来电 | 只通知分配了该模块、注册有效、开启接听且空闲的 SIP 用户；忙碌用户跳过。 |
| 多人空闲 | 同时响铃；首个有效接听者独占通话，其余停止响铃。迟到的 SIP 200 OK 仍须 ACK 后 BYE。 |
| 无人可接 | 所有人忙碌、离线、关闭接听，或都拒接/响铃超时，结束新来电，不影响其他通话。 |
| 模块已占用 | 拒绝该模块第二通来电，不启用呼叫等待，不打断原电话。 |
| 网页 | 只拨出、不接来电，与 SIP 共用模块占用管理。 |
| 账号状态 | 在线、通话中、离线。通话中包括拨号、响铃及清理中的资源占用；以真实注册及呼叫事件为准。 |
| 局域网 | 未配置或已注销 SIP 服务器时使用局域网地址，允许 1024–65535 中的可用端口，不套用云端范围。云端连接中/故障/状态未知不擅自切换线路；已有账号端口不自动改写。 |
| 端口 | 局域网端口框显示“输入端口”；云端模式在输入框内以占位文字显示服务器分配范围，不固定 5060、不预填端口；编辑保留原值。配置刷新不覆盖输入，读取失败不猜范围，前后端拒绝越界和保留端口。 |
| 界面 | 复用当前页面，状态列位于模块和操作之间，不显示模拟在线或模拟接通。 |

关闭接听只停止该账号的待接分支，不打断已经接通的电话。若其他用户仍在响铃，不能连带挂断他们的来电。

## 通话承载与网络隔离

- 同一套 SIP 账号/调度规则分别接蜂窝语音和 VoWiFi/IMS 两种适配器。两条线路各自验收，不能把短信成功、网络在线或 IMS 注册成功作为通话验收。
- SIP 电话服务器是远程 SIP 客户端的接入通道，不是全局代理；承载 SIP 注册、续期/保活、呼叫信令和通话媒体，VPN 自身也有握手/保活。
- 空闲时保持注册与 VPN 所需的小流量以便接听，不实现为只有拨打时才连接。
- 短信、彩信、eSIM、网页管理、更新、主机服务器和其他主机流量继续走原路径，不绑定 SIP VPN 地址。不修改主机默认路由和 DNS。
- VoWiFi 到运营商 ePDG/IMS 的网络与 SIP 客户端到主机的 VPN 是两段不同链路；不能因名称都有 SIP/IMS 就合并路由。
- 现有 `deploy/sip-network.py` 仅对 VPN 源地址配置专用路由表。接入电话服务时延续该方式，并验证业务流量与默认路由未被接管。

## 实现进度

- [x] 账号 CRUD、注册、端口范围、模块分配和真实在线/通话状态。
- [x] 蜂窝 SIP 拨出及双向 G.711/USB PCM；用户确认 v1.6.10 句尾声音正常。
- [x] Wi-Fi SIP 拨出接入调度、私有 PCM 流和双向 G.711 RTP；运营商实机验收单独记录。
- [x] 呼叫元数据持久化，不录制通话。
- [ ] Wi-Fi 运营商实际拨出、接听后双向声音及重复呼叫验收。
- [ ] 仅支持 AMR/AMR-WB 的运营商编码适配；不宣告尚未实现的编码。
- [ ] 来电首接、真实通话记录页面、网页拨出。
- [ ] 顶号、改密、删除的实机立即挂机验收；服务器撤权不等于控制第三方 APP 返回登录页。
- 每次发布独立核验 GitHub、同提交标签、两架构 Latest 安装包及主机；自动测试不替代运营商实测。

## 调度核心接入约定

`backend/internal/telephony` 是内部调度核心，不监听 SIP、不认证网络请求，不发送实际电话。蜂窝和 Wi-Fi SIP 拨出已接入此核心；来电和网页驱动仍待接入，不因通过调度测试就宣告这些功能可用。

- `Register` 只能由完成真实 SIP 认证的适配器调用，携带数据库凭据版本。`Refresh` 只续期原注册 ID；注册 ID 不是网络鉴权令牌。旧注册自动续期不得变成新的登录顶回 B。
- SIP 的 REGISTER 同时用于初次注册和续期，需在适配器中绑定真实客户端实例、连接和鉴权 nonce，处理重传及 CSeq；不能每次 REGISTER 都调用 `Register`。普通 SIP 无通用的“让 APP 返回登录页”命令，只能保证服务器端撤权。
- 账号修改/删除与注册认证须使用同一账号级事务/版本栅栏；不得让数据库写入失败变成假成功。重启后注册和占用不能从缓存恢复为在线，先与模块核对并清理孤立呼叫。
- 所有操作返回待执行 `Action`。驱动串行处理状态事件和命令，执行前用 `ActionCurrent` 丢弃已被顶号/挂机取代的拨号、响铃和接听。即使返回错误，也必须处理返回的清理动作。
- `StopMedia` 先停止本地转发，再发送终止信令。撤权即刻生效；模块及客户端的清理确认前不释放模块占用。清理失败保持隔离，绝不用定时器假设已挂断。
- `ClientClosed` / `ModuleClosed` 仅由真实结束事件或核实后的清理结果驱动。`Connected` 仅由通话及媒体均已就绪的驱动调用。ACK、重传等协议细节由适配器负责。
- `Tick` 使用单个服务级定时器处理注册过期、拨号/响铃超时，不为每行列表开计时器。前端轮询只读状态，不负责释放占用。
- 模块必须明确报告语音可用才加入选择池；“在线”或“IMS 注册成功”不等于支持当前编码的双向通话。

## 已发现的接入风险

- Wi-Fi 通过私有控制与独立 PCM 流接入 SIP 调度，媒体在运营商确认接听、编码协商和内部音频流就绪后才开放。实际运营商收发声音仍须验收。
- RTP 协商已收紧为实际具备的单声道 8 kHz G.711；不宣告未实现的 AMR/AMR-WB 或电话事件。仅接受 AMR 的运营商仍需真实编码器和实机验收。
- 云端 VPN 已有网络配置接口，真正 SIP 信令与 RTP 均需使用其返回的端口范围，分开预留监听端口和媒体端口，避免冲突。
- 不清空用户会话、短信、彩信、SIM 配置或回滚备份；本次由用户 APP 自行拨打验收，不自动呼叫或改变现有模块射频配置。

## 注册阶段验收（历史阶段，现已接入拨出）

- SIP 服务按账号端口监听 UDP/TCP；局域网仅绑定本机私网 IPv4，云端仅绑定专用 SIP VPN 地址。不开通 TLS/全局代理，不修改防火墙、默认路由或 DNS。
- 注册有效期最多 300 秒，过期离线；同一账号新客户端替换旧客户端。旧客户端的同一 Call-ID/实例续注册被拒绝一小时；改密、改用户名/端口、删除即时撤销当前注册。服务重启后全部重新鉴权，不从缓存恢复在线。
- 普通 SIP 的新 REGISTER 与重新登录没有统一 UI 语义；被替换 APP 主动创建全新实例并持有有效密码时视作新的认证登录。此限制不能用“手机自动退出登录页”表述掩盖。
- 通话承载尚未接入，已认证 INVITE 明确返回 503，不伪造 200/接通；当前版本只可验收账号管理和手机注册。
- 自动测试覆盖 UDP/TCP 实际套接字、双摘要算法、错误密码、源地址/端口隔离、防重放、过期、顶号、改密、删除、端口冲突及配置切换。

## v1.6.2 兼容检查

实机抓到 Zoiper v2.10 和 PortSIP iOS v12 的未鉴权 REGISTER 重试。针对这两类旧客户端仅下发已支持的 MD5 挑战，其余客户端继续优先 SHA-256；不因 User-Agent 跳过密码、来源、端口或防重放验证。真实手机完成 200 OK 注册前不报告登录成功。

## v1.6.3 通话驱动基础

- 仅新增私有控制接口，不新增公开拨号 API，不开放真实 SIP INVITE；callsReady 仍为 false。
- IMS 挂机先停止 PCM，保留 ending 到有效 INVITE 终结/BYE 确认；CANCEL 200、超时及丢失 IPC 回复均不释放模块。
- 迟到 200 必须 ACK/BYE；重传 200 重发 ACK。来电重传复用会话，晚到 CANCEL 不结束已接听的会话；同模块第二通来电拒绝。
- 后续先完成音频 IPC、EC20 驱动和 SIP 对话桥，再接现有调度、账号撤权及真实记录；接听 ACK/超时、媒体中断和手机双向声音仍需专项验收。
- 按 RFC 3261 第 9、13、15 节实现取消/结束语义；本项目对清理结果采用更保守的隔离策略。

## v1.6.4 蜂窝拨出接入（实机验收另行记录）

- SIP INVITE 经当前注册、Digest 和模块授权后进入真实调度；仅选择有 USB PCM 接口的 EC20 蜂窝模块。Wi-Fi 呼叫和网页拨号仍保持未开放，不把它们算作已完成。
- 复用 G.711 RTP，引入 USB NMEA 8 kHz PCM 双向转发；媒体显式绑定 SIP 网络地址并从服务器范围分配端口，不修改默认路由或 DNS。
- CLCC 的 LTE 数据上下文不计作语音占用；CHUP 结束语音，清理后恢复 PCM/GPS 输出设置。超时的旧 AT 响应不得确认新挂机；未确认清理保持占用。
- SIP 账号状态接真实调度；呼叫元数据写入 sip_call_records，不存录音。记录页面接入及来电分支仍待完成。账号接口 callsReady 只表示当前有蜂窝拨出候选，incomingReady 仍为 false；最终能力以建立呼叫和双向声音为准。
- 用户只批准一通 01 → 02，接通后不超过 20 秒；未进行之前不得写成已验收。自动测试中的替身驱动只用于协议与资源管理回归，不代表实机声音通过。

## v1.6.5 接听即挂断修复

- 根因是 INVITE 处理器先返回，sipgo 提前终结事务；后续 200 虽已发送，Respond 仍返回 transaction terminated，驱动随即清理模块。
- 处理器等待通话工作任务结束，等待期间释放账号锁；ACK、BYE、账号撤权继续正常处理。兼容事务内及新分支 ACK。
- 真正 UDP/TCP 套接字覆盖接听、双向 RTP、BYE 和资源释放；把旧生命周期放回后可复现失败。替身模块回归不替代实机音频验收。
- 短暂模块轮询占用改为有界等待；保留事务和媒体失败原因日志，不记录声音或密码。
- 用户追加授权必要的重复测试，可拨打指定尾号 3307；每次接通测试保持在 20 秒内，不录音。此前两次接听后自动挂机不计为通过。

## v1.6.7 蜂窝音频连续性（音质待实机复测）

- USB 上行改为每 100 ms 写入 800 个 16 位样本，启动时预填两块静音留出一块余量；短包/断流补零，最多缓存 300 ms 网络样本，积压丢最旧样本。撤权和错误取消读任务，不在后台继续送音。
- 从拨号前开始读取模块 PCM；ACK 之前丢弃回铃，ACK 之后才向 SIP 客户端转发，避免旧回铃积压数秒后突发回放。
- 自动测试使用虚拟时钟验证突发输入、完整样本顺序、静音输出、缓冲上限、取消和读写错误；G.711 解码对照独立参考校验全部 256 个码字。不把测试替身当实机验收。
- 录音分析确认正常 400 Hz 提示音夹杂约 0.7 秒间隔的短瞬态。此前一次主机 USB 写入抓取的 120 块数据均为预期静音/测试音，写入完整，间隔约 97–104 ms；未证明全部杂音均由网络、模拟麦克风或某一增益设置引起。
- 模块 02 的 QAUDMOD=3 和 ALC5616 上行最低增益试验未消除用户反馈的杂音，均已恢复原值。本版不永久修改这些参数。
- 最新用户测试要求：更新后用模块 02 拨打其指定尾号 3307，15 秒静音、10 秒提示音、15 秒静音，约 40 秒后挂断；用户用 iPhone 录音并提供分析。该次明确要求替代早前短测时限，主机仍不自动录制通话。测试前等待发布/主机同步，不反复拨打旧版本。
- 双向说话、静音段和重复呼叫的实机音质尚未验收；不因一次“好像没有了”就报告修复完成。

- v1.6.6 候选在新测试的观测数据共享上触发竞态检查，未发布安装包、未部署主机；v1.6.7 改用通道采集测试结果，并保留完整竞态检查。

## v1.6.8 模拟输入隔离与恢复

- 模块 02 对照测试：仅将 QMIC 第一项从 20577 改为 0，第二项保持 14567。内部 WAV 测试用户反馈正常；后续实际 SIP/RTP → USB → 蜂窝的 40 秒测试录音也没有此前明显的静音风声和周期瞬态。不是关闭 APP 麦克风，不据此宣告双向说话已验收。
- 生产驱动读取每个模块自己的参数，不写死上述数值。持久保存原 QMIC、PCM、GPS 后再设置；读回不一致或格式不支持则停止呼叫，保留恢复记录。
- 挂机先确认语音结束，再按当前值恢复；准备和清理期间继续丢弃 USB 残留数据，已关闭的 PCM 不重复关闭。数字增益、音频模式及 ALC5616 配置保持不变。
- 恢复记录绑定 IMEI，而不是 SIM、账号或临时 USB 端口。正常结束删除记录；进程中断后在模块轮询或下一通前重新验证身份并恢复，失败保持不可拨号状态。记录不含录音、手机号或凭据。
- 第 16 次旧版测试通话结束后出现 AT 清理阻塞；临时增益经独立恢复任务恢复，随后已核实语音空闲、PCM=0,0、GPS=usbnmea。新版的实机重复呼叫与恢复还需单独核实，不以自动测试替代。

## v1.6.9 模拟麦克风常关

- 用户已确认 v1.6.8 正式主机上的 APP 蜂窝通话正常；模块 02 最近一次真实通话记录已完成接听及结束。服务中断恢复也已单独验收；未做整机断电重启，不混同验收结果。
- 用户明确不使用模块外接模拟麦克风，改为常关。空闲轮询、Wi-Fi 工作进程启动及拨号准备读回核对 QMIC；只将第一项设为 0，第二项保留当前数字增益。已关闭不重复写入，不修改通话中的模块。
- 挂机、取消、失败和旧版恢复记录不再恢复模拟麦克风；删除 per-call Mic 快照，PCM/GPS 的持久恢复和身份校验保持不变。轮询遇到设置失败不报告可拨号，后续重试。
- 不关闭 APP 麦克风，不改变现有 SIP 数字音频；常关不代表 Wi-Fi 通话媒体已经接通。Wi-Fi SIP 拨号和音频接入仍是独立未完成项。
- 当前按用户要求由用户在 APP 自行拨打验收，不自动继续呼叫其号码。

## v1.6.10 句尾断续排查与缓冲优化

- 用户在 v1.6.9 的实际 APP 蜂窝通话仍反馈句尾轻微咔哒、断续。旧日志没有 RTP/USB 数量统计，尚未确认这通电话的具体根因；不能直接认定为网络问题或宣告实机修复。
- 原 RTP 按到达顺序排队，不检查序号/时间戳。改为固定 512 ms 样本窗口、60 ms 初始抗抖动余量、20 ms 播放节拍；按时间戳拼接，兼容序号/时间戳回绕和不同包长，去重、丢弃迟到/其他 SSRC 数据。每通话固定一个 SSRC，不借此宣称新增了保持/媒体重协商支持。
- 空档补零且保留原时间线，不把后续语音提前填进丢失部分，不重复播放旧语音。超过一秒的停顿允许同一音频流重新对齐；读端卡顿时丢弃过旧时段，避免无限延迟或大段突发回放。
- USB 首段不足 800 个样本时先送静音、保留收到的样本，攒齐后开始；后续仍按既有 100 ms 节拍输出。有效 PCM 样本不变，不增加降噪、自动增益、变声、门限或音量增强。
- 每通话结束记录一次数字统计：RTP 乱序/重复/迟到/时间窗外/补静音，以及 USB 预填/缺样本/溢出/迟写/流控丢弃。无录音、无音频文件、无手机号或凭据；补静音也可能来自客户端静音抑制，不标为已证实丢包。
- 回归包含虚拟时钟、真实 UDP、回绕、不同包长、初始乱序、断流、取消及模糊测试。正式主机更新后仍由用户 APP 复测句尾及双向说话，不自动拨打其号码。
- 时间线与序号语义依据 RFC 3550 第 5.1 节：https://www.rfc-editor.org/rfc/rfc3550.html#section-5.1 。本实现不声称已经接入 RTCP、AMR 或自适应编解码。

## v1.6.11 Wi-Fi SIP 拨出与音频

- 用户已确认 v1.6.10 蜂窝 APP 通话声音正常。保留该路径与模拟麦克风常关策略，不增加降噪、自动增益或音量增强。
- 复用既有 IMS 注册、身份和运营商路由；Wi-Fi 候选须匹配当前模块代次、SIM、工作进程会话、注册与 RF 关闭状态。关闭、重连、失联或切卡时不偷偷改用蜂窝。
- Wi-Fi 注册期间硬件 gate 仍由原工作进程持有，SIP 只通过私有接口操作，不争抢串口、不改默认路由和 DNS。APP RTP 保留 SIP 专用网络，运营商 RTP 仍绑定 IMS 隧道内地址，两段网络不混用。
- 增加每通话独占的 Linux abstract UNIX 音频流。地址与认证密钥分别随机生成；32 字节密钥仅经已有私有控制通道传递，认证与待拨号均有时限，不公开 TCP 监听或任意路径接口。
- 独立帧传输 20 ms/160 样本、16 位单声道 8 kHz PCM；复用 RTP 抗抖动缓冲，不经过 USB 的 100 ms 预填。帧长、队列与等待均有界，SMS/MMS 存储/大包不阻塞媒体通道。
- 同一流只允许拨号一次，禁止第二控制器越过占用；挂断、撤权、媒体断开立即停止转发，只有运营商终结确认后才释放。丢失媒体终结 ACK 时通过原工作进程的流 ID 与呼叫状态核实；未确认保留占用，不自动重拨。
- 媒体只宣告已实现的 PCMA/PCMU，分别协商 APP 与运营商格式；拒绝无共同编码时不伪造接通。依据 RFC 3264 offer/answer 与 RFC 3551 G.711；本版不新增 AMR/EVS、保持、来电或网页拨号。
- 回归覆盖真实本地 UNIX 流、密钥错误、代次/SIM 隔离、双向原样 PCM、SMS 控制阻塞、断线挂断、未知结束占用、撤销会话、帧边界模糊测试及 SIP/RTP 双向套接字。替身运营商测试不计作模块 03/04 的实机验收。

## v1.6.12 Wi-Fi 接听后断开排查

- 用户报告接听即挂断。03 的两次独立 SIP 测试在运营商 200/PCMU 和 APP 200 后约 0.4 秒结束，日志为音频状态错误。原 IMS 专用表仅有 P-CSCF 路由，缺少 SDP 指定的 RTP 端点；不能把 IMS 注册成功作为音频网络已就绪。
- 为已协商的媒体申请精确主机路由，只使用当前隧道源地址与表；验证 IPv4/IPv6、实际本地地址、UDP 端口和双向流量选择器。共享地址计数释放，不移除原注册路由；失败保持不可用，不回退主机网络。
- 媒体重协商先申请新路由，成功后释放旧路由；通话关闭释放租约，隧道关闭清理遗留路由。保持 ESP 逐包选择器检查，不加全局默认路由或放宽为所有目标。
- 自动测试另复现可靠 183 已协商而最终 200 没有 SDP 的兼容缺陷。只在同一 To-tag 复用已确认格式；这不是上述实机通话已证实的根因。补充失败阶段枚举及正确的最终 SIP 状态，不保存原始信令或录音。
- 用户允许短测，最新目标为其提供的尾号 6188；不再拨先前号码。独立测试账号、单次 INVITE、接通后最多 20 秒；保留正式账号现有分配。实测通过前不宣称 Wi-Fi 音质正常。
