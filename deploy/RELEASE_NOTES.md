# v1.5.2

- 修复美国号码缺少 `+`：读取 SIM 国际号码类型，并使用同一张卡、相同数字的 IMS 国际号码校正显示。
- 去掉裸 `133…` 等号码自动判断为中国号码的规则。
- 按 SIP 标准处理 REGISTER 423 / Min-Expires，保留续期要求，注销保持 Expires: 0。
- 包含此前主机热修复：3HK ePDG 网关识别，以及 Wi-Fi 会话中因过期快照导致 eSIM 任务被误拦截的问题。
- 增加版本标签自动发布流程，测试成功后发布 amd64、arm64 和校验清单，一键部署读取 Latest。

已知限制：3HK 卡仍未在当前主机完成 IMS 注册；本版本不宣称全部运营商、实际电话音频或 MMS 收发均已适配。
