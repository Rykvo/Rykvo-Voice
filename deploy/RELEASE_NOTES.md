# v1.5.16

- 自动识别 ISIM 后，发起短信使用本次注册响应的默认业务身份，不再优先沿用 ISIM 注册身份；不修改注册认证，不伪造号码或身份。
- 显式运营商身份配置、接收确认回复及 USSD 保留原规则；续期后立即使用新的网络默认身份，缺少有效身份时不提交短信。
- 保留 v1.5.15 的 UCS2 短信中心解析修复，以及气泡下方转圈、已送达、红色尚未送达三种显示。
- SMS 投递与回执、MMS 及 3HK 兼容性依据实卡验收，不把 SIP 接受或 RP-ACK 当作已送达。
