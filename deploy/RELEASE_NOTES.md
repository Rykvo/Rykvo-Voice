# v1.5.17

- 蜂窝短信启用 CNMI 存储型状态报告，补读独立 SR 回执存储区；不删除模块短信，读取后恢复原存储选择。
- 修正短信回执匹配的时间边界：按每段实际提交时间双向校验，旧短信重复编号不再长期阻挡新回执；仍有歧义时不标送达。
- 保留 v1.5.16 的网络默认业务身份修复。03、04 已实测双向收件，并收到 status=0 的成功送达回执。
- SMS 已受理后继续转圈等待回执，2 分钟仍未确认则显示尚未送达；迟到的成功回执仍会更新为已送达，不自动重发。
- 界面只保留气泡下方转圈、已送达、红色尚未送达；网络受理不等于送达，不显示“已发送”。
- 不重发历史未确认短信，不清理消息或用户设置；3HK、MMS 未标记完成。
