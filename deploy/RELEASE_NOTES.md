# v1.5.7

- 模块页新增「模块重启」「主机重启」及每行独立「重启」。
- 复用圆角确认框：取消蓝色、重启红色，默认聚焦取消。不显示“原设置保留”。
- 全部模块依次提交重启；单模块只操作对应设备。保留 Wi-Fi 通话、漫游、账号与卡配置。
- 操作持久化去重，设备忙时拒绝。模块重新枚举且同一 IMEI 读取正常才确认恢复；主机重启延迟执行。
- 3HK 注册适配和 MMS 实卡收发仍未完成，本版本不宣称修复。
