# v1.5.13

- 修复 EC20 图片上传因非阻塞串口短暂忙碌而中断；有界等待并继续未写入字节，不丢失或重复附件数据。
- 保留原图内容、分块校验、明确的上传错误阶段；实机同一图片连续 10 次完整写入及回读通过。
- 彩信接收缺少专用网络时保持等待，不误标永久失败；发送提交结果不明时仍禁止自动重发。
- 保留消息、备注、SIM 及 Wi-Fi Calling 设置。文件测试不代表运营商发送/接收验收；3HK IMS 与全运营商兼容仍未完成。
