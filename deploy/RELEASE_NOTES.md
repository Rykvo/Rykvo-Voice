# v1.6.7

- 蜂窝 SIP 上行增加固定节奏的 USB PCM 播放缓冲；静音和短暂断流补零，限制积压，不直接按网络包到达时刻写入模块。
- 从拨号开始读取 USB 音频，丢弃接通前回铃，避免接听后突发回放积压声音。
- 增加送音节奏、短包补零、积压上限、撤权停止和读写失败清理测试；校验 G.711 解码的全部码字。
- 不改外接麦克风增益、模块持久音频配置、账号、默认路由或 DNS。

本版处理媒体缓冲缺口；录音中的敲击声和静音底噪仍需更新后实机复测，不宣告音质问题已全部解决。Wi-Fi 音频、来电及网页拨号仍未完成。
