# v1.6.9

- 模块模拟麦克风改为常关，保留当前数字增益；不关闭手机 APP 麦克风或 SIP 数字音频。
- 空闲模块轮询及 Wi-Fi 工作进程启动时核对设置，异常重置后重新关闭；已关闭不重复写入，不打断已有通话。
- 挂机和异常恢复仅还原 PCM/GPS，不再恢复模拟麦克风；旧版恢复记录兼容读取，不重新开启模拟输入。
- 增加常关、重复轮询、模块重置、在途通话保护、数字增益保留、旧记录迁移和读回失败测试。
- 保持现有页面、账号、短信彩信、默认路由和 DNS 不变。

用户已确认 v1.6.8 实际 APP 蜂窝通话正常。本版按用户要求保持模拟输入关闭；Wi-Fi 音频、来电及网页拨号仍未完成，不能据此视为已支持。
