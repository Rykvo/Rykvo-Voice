# v1.6.0

- SIP 账号接入数据库，支持创建、修改、模块分配、删除及真实注册在线状态；不返回或缓存密码。
- 接入 UDP/TCP SIP Digest 注册、续期、防重放及单客户端替换；改密/删除撤销注册。
- 局域网端口框显示“输入端口”；云端动态显示服务器分配范围，拒绝越界、保留及已占用端口。
- 保持 SIP 网络隔离，不修改主机默认路由、DNS、SIM、短信或彩信配置。
- 本版本为 SIP 登录测试阶段；实际拨打、接听和双向音频尚未接通，INVITE 明确返回未就绪，不伪造接通。
