# v1.6.4

- SIP 客户端蜂窝拨出接入 EC20：校验当前注册和模块分配，建立实际语音呼叫并双向转发 USB PCM/G.711 RTP。
- 呼叫占用进入共享调度，顶号、改密和删除触发停止音频与挂机；未确认清理保留占用。
- LTE 数据上下文与语音呼叫分开判断，使用语音挂机指令；通话结束恢复 PCM/GPS 设置，不修改默认路由或 DNS。
- 媒体端口从服务器分配范围选择，局域网独立绑定；账号状态取实际调度，呼叫元数据持久保存。
- 增加鉴权、真实 UDP 双向媒体、挂机清理和迟到 AT 回应测试；保留现有账号、短信彩信、SIM 和页面样式。
- 当前只开放 EC20 蜂窝 SIP 拨出；VoWiFi 音频桥、SIP 来电分支、网页拨号和记录页面尚未完成。实机声音与运营商兼容性须单独验收，不以自动测试代替。
